# TODO
- play queue:
  1) fetch queue from server
  2) now we have list of track ids
  3) we start play -> fetch track info
  4) change tab title, change cover in home
  5) if queue is empty: fetch new one from server
